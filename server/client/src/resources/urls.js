// request/server urls
const host = window.location.origin.toString();

export const authURL = host + '/api/auth';
export const logdataURL = host + '/api/logdata';
export const dbstatsURL = host + '/api/dbstats';
